# Publish Postgres to localhost only (for host-network app). Default 15432 avoids conflict with system postgres on 5432.
services:
  hodhod-db:
    ports:
      - "127.0.0.1:${HODHOD_DB_HOST_PORT:-15432}:5432"
